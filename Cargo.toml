[package]
name = "valkey-cuckoo"
version = "0.1.0"
authors = [
  "Sam Shaplygin <semyuon@gmail.com>",
  "Seif Lotfy <seif.lotfy@gmail.com>",
  "Seif Lotfy <seif@axiom.co>",
  "Florian Jacob <accounts+git@florianjacob.de>",
  "The cuckoofilter contributors",
]

# A short blurb about the package. This is not rendered in any format when
# uploaded to crates.io (aka this is not markdown)
description = "Cuckoo filter with injectable RNGs for deterministic eviction; a fork of cuckoofilter"

# These URLs point to more information about the repository
homepage = "https://github.com/sshaplygin/rust-cuckoofilter/tree/release/valkey-cuckoo"
repository = "https://github.com/sshaplygin/rust-cuckoofilter"
documentation = "https://docs.rs/valkey-cuckoo"

# This points to a file in the repository (relative to this Cargo.toml). The
# contents of this file are stored and indexed in the registry.
readme = "./README.md"

# This is a small list of keywords used to categorize and search for this
# package.
keywords = ["bloomfilter", "cuckoohashing", "cuckoofilter"]

# This is a string description of the license for this package. Currently
# crates.io will validate the license provided against a whitelist of known
# license identifiers from http://spdx.org/licenses/. Multiple licenses can
# be separated with a `/`
license = "MIT"

edition = "2018"

[lib]
name = "cuckoofilter"

[features]
default = []
serde_support = ["serde", "serde_derive", "serde_bytes"]

[dependencies]
byteorder = "1.4.3"
rand = "0.8.5"
serde = { version = "1.0.137", optional = true }
serde_derive = { version = "1.0.137", optional = true }
serde_bytes = { version = "0.11.6", optional = true }
fnv = "1.0.7"
farmhash = { version = "1.1.5", optional = true }

[dev-dependencies]
rand_chacha = "0.3"
serde_json = "1.0.81"
